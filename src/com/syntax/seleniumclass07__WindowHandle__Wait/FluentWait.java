package com.syntax.seleniumclass07__WindowHandle__Wait;
//https://www.selenium.dev/documentation/en/webdriver/waits/

/*In Fluent wait, you can customize the apply method 
 * and you can write your own conditions based on your requirement
 * apply method will be like clickable, and visible
 */
public class FluentWait {

	/*
	 * Fluent Wait: The fluent wait is used to tell the web driver to wait for a
	 * condition, as well as the frequency with which we want to check the condition
	 * before throwing an "ElementNotVisibleException" exception. 
	 * 
	 * FluentWait wait =new FluentWait(driver); 
	 * wait.withTimeout(Duration.ofSeconds(30));
	 * wait.pollingEvery(Duration.ofSeconds(1));
	 * wait.ignoring(NoSuchElementException.class);
	 */
}
